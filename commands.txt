backend\venv\Scripts\Activate
cd backend/
uvicorn app.main:app --reload

cd frontend/
npm run dev

cd backend/
venv\Scripts\Activate
celery -A app.core.celery_app.celery_app worker --loglevel=info --pool=eventlet

pip install -r requirements.txt

run redis for db on docker
run mailhog for mail on docker


doppler run -- docker compose -f docker-compose.yml up --build
doppler run -- docker compose -f docker-compose.yml down
